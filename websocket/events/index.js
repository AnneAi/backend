const connectStudent = require('./connectStudent');
const disconnect = require('./disconnect');
const init = require('./init');
const message = require('./message');
const typingOff = require('./typingOff');
const typingOn = require('./typingOn');

module.exports = {
  connectStudent,
  disconnect,
  init,
  message,
  typingOff,
  typingOn
};
